#!/bin/sh

# Get root directory of project.
ROOT="`cd ${0%/*} && echo $PWD`/.."

# Change to resources directory.
cd "$ROOT/resources"

# Start consize.
java -cp `lein classpath` clojure.main "$ROOT/src-clj/consize.clj" "\ prelude.txt run say-hi"
