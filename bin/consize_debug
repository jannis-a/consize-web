#!/bin/sh

ROOT="`cd ${0%/*} && echo $PWD`/.."
CONSIZE="$ROOT/resources/public/consize"

cd ${CONSIZE}
java -cp ${ROOT}/lib/clojure-1.5.1-slim.jar clojure.main consize_debug.clj "\ prelude.txt run say-hi"
